{
  "name": "getinside-handbook",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build && node scripts/generate-sitemap.js && node scripts/generate-structured-data.js && node scripts/generate-llms-txt.js",
    "docs:preview": "vitepress preview",
    "generate:og": "node scripts/generate-og-image.js",
    "generate:icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vitepress": "^1.5.0"
  }
}
