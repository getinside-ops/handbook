source 'https://rubygems.org'

# Utiliser la version exacte des dépendances de Github Pages
gem 'github-pages', group: :jekyll_plugins

# Pin ffi for Ruby < 3.0 (macOS system ruby)
gem 'ffi', '~> 1.15.5'

group :jekyll_plugins do
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end
