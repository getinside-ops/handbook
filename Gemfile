source "https://rubygems.org"

# Installe les dépendances standard de GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Installe le plugin nécessaire pour utiliser le thème à distance
group :jekyll_plugins do
  gem "jekyll-remote-theme"
end